name: kigali_city_directory
description: Kigali City Business Directory application.
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^3.3.0
  firebase_auth: ^5.1.4
  cloud_firestore: ^5.4.4
  provider: ^6.1.2

  google_maps_flutter: ^2.7.0
  url_launcher: ^6.3.0

  geolocator: ^12.0.0
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  firebase_core: ^3.3.0
  firebase_auth: ^5.1.4
  cloud_firestore: ^5.4.4
  provider: ^6.1.2
  url_launcher: ^6.3.0
  geolocator: ^12.0.0
  shared_preferences: ^2.3.2
  flutter_map: ^7.0.2
  latlong2: ^0.9.1

flutter:
  uses-material-design: true